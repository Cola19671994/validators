/*export const passwordSymbolsValidator = (value) =>
  /^\S+$/.test(value) &&
  /[a-zA-Z]+/.test(value) &&
  /[0-9]+/.test(value) &&
  /\W+/.test(value)
    ? null
    : "Password must have letters, symbols and numbers";
    */
